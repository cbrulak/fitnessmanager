<table>
  <tr>
    <th>Name</th>
    <th>Reps</th>
    <th>Numsets</th>
  </tr>

<% @workout_sets.each do |workout_set| %>
  <tr>
    <td><%= link_to workout_set.name, workout_set %></td>
    <td><%= workout_set.reps %></td>
    <td><%= workout_set.numSets %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Workout set', new_workout_set_path %>
